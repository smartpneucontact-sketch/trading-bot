[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# Cron job: run at 9:35 AM ET (13:35 UTC) every weekday
# Market opens 9:30 — we wait 5 min for opening prices to settle
cronSchedule = "35 13 * * 1-5"
restartPolicyType = "NEVER"
